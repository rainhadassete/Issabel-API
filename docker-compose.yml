version: '3.8'

services:
  api:
    build: .
    container_name: issabel-api
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    networks:
      - issabel-net

networks:
  issabel-net:
    driver: bridge
